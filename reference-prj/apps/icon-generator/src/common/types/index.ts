import * as BABYLON from '@babylonjs/core';

export interface IIconSetting {
    fov: number;
    alpha: number;
    beta: number;
    radius: number;
    lookTarget: BABYLON.Vector3;
    iconSize: number;
}
